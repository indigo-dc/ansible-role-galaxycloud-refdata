---
# Create directories for reference data and logs

- name: Create directories
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ galaxy_user }}"
    group: "{{ galaxy_user }}"
  with_items:
    - '{{ refdata_dir }}'
    - '{{ refdata_log_path }}'
    - '{{ galaxy_log_path }}'
  become_user: root
  become_method: sudo

#- include: install_galaxyctl.yml
